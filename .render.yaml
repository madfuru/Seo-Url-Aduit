services:
  - key: PPTX_TEMPLATE
    value: /opt/render/project/src/templates.pptx
  - type: web
    name: seo-url-audit
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
