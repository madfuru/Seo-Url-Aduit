services:
  - type: web
    name: seo-audit-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: PPTX_TEMPLATE
        value: /opt/render/project/src/template.pptx
